# Frontend

Next.js app for RoundVault. See the [root README](../README.md) for setup, environment variables, and deployment.

```bash
npm install
npm run dev
```

Generate local env from the deployment manifest (run from repo root):

```bash
npm run sync:env
```
